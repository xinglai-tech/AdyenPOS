node_modules/
.env

# Receipt customisation done at runtime; assets/receipt-logo.png is the default logo.
assets/receipt-logo-custom.png
assets/receipt-settings.json

# Loyalty members edited at runtime; assets/members.default.json is the seed data.
assets/members.json

# Self-signed certificate for local HTTPS; regenerate with `npm run certs`.
certs/

.DS_Store
**/.DS_Store
