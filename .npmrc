# Install from public npm even when the machine is configured to use Adyen's
# internal Artifactory mirror: those URLs end up in package-lock.json and the
# GitHub Actions runner cannot reach them, which fails the deploy build.
registry=https://registry.npmjs.org/
